option_settings:
  # The first deploy that pulls @google-analytics/data (grpc + protobuf) makes the
  # on-instance `npm install` slower than the default 600s command window, so EB
  # aborts the deploy with "TimedOut". Give the install room to finish.
  aws:elasticbeanstalk:command:
    Timeout: 3600
